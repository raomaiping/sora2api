version: '3.8'

services:
  sora2api:
    image: ghcr.io/raomaiping/sora2api:latest
    container_name: sora2api
    ports:
      - "8090:8090"
    volumes:
      - ./data:/app/data
      - ./config/setting.toml:/app/config/setting.toml
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
